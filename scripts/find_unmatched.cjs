const fs=require('fs'); const s=fs.readFileSync('src/App.jsx','utf8'); const braceStack=[]; const parenStack=[]; for(let i=0;i<s.length;i++){ const ch=s[i]; if(ch==='\'' || ch==='"'){ const quote=ch; i++; while(i<s.length && s[i]!==quote){ if(s[i]==='\\') i++; i++; } continue; } if(ch==='`'){ i++; while(i<s.length && s[i]!=='`'){ if(s[i]==='\\') i++; i++; } continue; } if(ch==='{') braceStack.push(i); else if(ch==='}') braceStack.pop(); if(ch==='(') parenStack.push(i); else if(ch===')') parenStack.pop(); } function line(pos){ return s.slice(0,pos).split(/\r?\n/).length; } console.log('unclosed braces (last 5):', braceStack.slice(-5).map(p=>({pos:p,line:line(p)}))); console.log('unclosed parens (last 5):', parenStack.slice(-5).map(p=>({pos:p,line:line(p)})));